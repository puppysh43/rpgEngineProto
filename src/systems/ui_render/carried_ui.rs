use crate::prelude::*;
//this function will display all of the carried items in the inventory that aren't equipped.
pub fn view_carried(state: &mut State) {
    //this will be similar to how inventory is handled in stuff like caves of qud
}
