use crate::prelude::*;

pub fn time_systems(state: &mut State, commands: &mut CommandBuffer) {
    //this will do all the different time related systems like bodies rotting or even time of day
    //first system will just calculate the time and then change the fov depending on the time
}
